# Memory extraction, connection discovery, follow-up questions.
# Without this the app falls back to its curated demo story and still runs end to end.
ANTHROPIC_API_KEY=
AI_MODEL=claude-sonnet-5

# Optional: shared pair state and audio storage.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
